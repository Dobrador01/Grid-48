{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for Grid 48 trusted app windows",
  "windows": ["main", "settings", "live-channels"],
  "permissions": ["core:default"]
}
